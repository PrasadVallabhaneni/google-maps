import React from 'react'
import { Map, GoogleApiWrapper } from "google-maps-react";

const Maps = () => {
    
    return (
        <div className="map">
            
        </div>
    )
}

export default Maps
